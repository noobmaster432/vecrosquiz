{
  "version": 2,
  "builds": [
    {
      "src": "index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "config": "/(.*)",
      "controllers": "/(.*)",
      "middleware": "/(.*)",
      "models": "/(.*)",
      "routes": "/(.*)",
      "dest": "/"
    }
  ]
}
